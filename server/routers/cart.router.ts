import {Router} from 'express';



const cartRouter = Router();

cartRouter.get('/:userId/get',);
cartRouter.post('/:userId/addToCart',)
cartRouter.post('/:userId/removeFromCart',)

export default cartRouter;